epic:
  epic-url: https://www.epicgames.com/store/en-US/
  driver-args:
    - "--blink-settings=imagesEnabled=false"
    - "--no-first-run"
    - "--disable-gpu"
    - "--no-default-browser-check"

spring:
  profiles:
    active: dev